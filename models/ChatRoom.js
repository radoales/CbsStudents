class ChatRoom {
  constructor(id, createdDate, name, chatMessages, chatImage) {
    this.id = id
    this.createdDate = createdDate
    this.name = name
    this.chatMessages = chatMessages
    this.chatImage = chatImage
  }
}

export default ChatRoom
